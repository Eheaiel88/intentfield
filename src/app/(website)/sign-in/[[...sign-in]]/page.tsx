import { AuthPage } from "@/components/auth-page";
export const metadata = {
  title: "Sign in — IntentField",
  robots: { index: false, follow: false },
};
export default function SignInPage() {
  return <AuthPage />;
}
